devServiceIp: 123.207.202.131

spring:
  profiles:
#    active: @env@ # 开发环境
    active: test # 开发环境
  cloud:
    nacos:
      discovery:
        server-addr: ${devServiceIp}:8848 #服务注册中心地址
        namespace: ${spring.profiles.active}