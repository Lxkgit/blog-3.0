#devServiceIp: @devServiceIp@

spring:
  profiles:
    active: @env@ # 开发环境
  cloud:
    nacos:
      discovery:
        server-addr: ${devServiceIp}:8848 #服务注册中心地址
        namespace: ${spring.profiles.active}